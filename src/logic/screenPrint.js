/**
 * Simply clears the console and then logs the provided text.
 * @param text {string}
 * @return {void}
 */
function clearAndPrint(text) {
  console.clear();
  console.log(text);
}

module.exports = clearAndPrint;